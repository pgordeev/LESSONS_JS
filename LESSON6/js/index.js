/* const myFavoriteFilm = {
  name: 'Достучаться до небес',
  prodDate: new Date(97, 0),
  director: 'Томас Ян',
  prodCountry: 'Германия',
};
myFavoriteFilm.fee = '$5710';
myFavoriteFilm.printName = () => {
  console.log(myFavoriteFilm.name);
}; */
// delete myFavoriteFilm.prodDate;

//  Реализовать функцию, которая будет принимать числовой диапазон [min, max] и
// будет возращать случайное целое число из данного диапазона

/* function getRnmNumber(min, max) {
  if (min > max) {
    [min, max] = [max, min];
  }
  const numLength = max + '';
  for (let num = min; num <= max; num++) {
    if (Math.floor(Math.random() * numLength.length) == num) {
      return num;
    }
  }
  return num;
}
 */

/* function checkLetter(str, n) {
  return str[n] <= str[n].toLowerCase ? true : false;
} */
